spring:
  test:
    database:
      replace: none
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:db_wallet
    username: sa
    password:
    driverClassName: org.h2.Driver

server:
  port: 8081